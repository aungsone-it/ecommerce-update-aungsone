# https://docs.netlify.com/configure-builds/file-based-configuration/
[build]
  command = "npm run build"
  publish = "dist"

# SPA routing: see public/_redirects (copied to dist/ by Vite) — /store, /admin, etc.
